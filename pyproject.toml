[project]
name = "trading-view-backtest"
version = "0.1.0"
description = "Python multi-symbol backtest harness for Pine Script strategies"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.2",
    "numpy>=2.0",
    "yfinance>=0.2.40",
    "tabulate>=0.9",
]

[tool.uv]
package = false
